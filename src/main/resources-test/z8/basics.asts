i32 __random__;
u8 zero;
u8 one;
u8 two;
u8 threeFour;

void printStringLength(u8* str, u8 length) {
	u8 chr;

	while (length > 0) {
		chr = *str;
		printChar(chr);
		length = length - 1;
	}
}

void printUint(i16 number) {
	u8* buffer[20];
	u8 pos;
	i64 remainder;
	u8 digit;

	pos = 20;
	while (true) {
		pos = pos - 1;
		remainder = (i64)(number % 10);
		number = number / 10;
		digit = ((u8)remainder) + 48;
		buffer[(i16)pos] = digit;
		if (number == 0) {
			break;
		}
	}
	printStringLength(&buffer[(i16)pos], 20 - pos);
}

void printChar(u8 chr) {
}

void main() {
	u8* onePtr;
	u8* twoPtr;

	printChar(zero);
	onePtr = &one;
	printChar(*onePtr);
	twoPtr = &two;
	printChar(twoPtr[0]);
	printUint((i16)threeFour);
	printChar(10);
}
