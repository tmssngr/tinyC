u8 zero;
u8 one;
u8 two;
u8 threeFour;

void printChar@u8(u8 chr) {
}

void printUint@u8(u8 number) {
}

i64 unusedArgs@u8@bool@u8@u8(u8 a, bool b, u8 c, u8 d) {
	return (i64)c;
}

void main() {
	i64 c;
	u8* onePtr;
	u8* twoPtr;

	c = unusedArgs@u8@bool@u8@u8(1, true, 2, 3);
	printChar@u8(zero);
	onePtr = &one;
	printChar@u8(*onePtr);
	twoPtr = &two;
	printChar@u8(twoPtr[0]);
	printUint@u8(threeFour);
	printChar@u8(10);
}
